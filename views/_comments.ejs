<li id="comment-<%= com.id %>">

    <p>
        <% if(locals.user && locals.user.id == com.user.id){ %>
            <a class = "delete-comment-button" href="/comments/destroycomment/<%= com.id %>">X</a>
        <% } %>   
        <%= com.content %>
        <br>
        <small>
            <%= com.user.name %>
        </small>
    </p>

</li>